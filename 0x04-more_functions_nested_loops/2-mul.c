#include "main.h"
/**
 * mul - multiplies two int(s)
 * @a: is the first int to be multiplied
 * @b: is the second int to be multiplied
 * Return: the result
 */
int mul(int a, int b)
{
	int r;
	
	r = a * b;
	return (r);
}
