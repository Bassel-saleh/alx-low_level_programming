#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @r: is variable
 * Return: is the absolute value of the int
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}
}
