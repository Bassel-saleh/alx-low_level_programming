#include "main.h"
/**
 * puts2 - print odds characters only
 * @str: is the string to be printed
 * Return: no return
 */
void puts2(char *str)
{
	while (s != '\0')
	{
		_putchar(s);
		s+=2;
	}
	_putchar('\n');
}
