#include "main.h"
/**
 * puts2 - print odds characters only
 * @str: is the string to be printed
 * Return: no return
 */
void puts2(char *str)
{
	while (str != '\0')
	{
		_putchar(s);
		str+=2;
	}
	_putchar('\n');
}
